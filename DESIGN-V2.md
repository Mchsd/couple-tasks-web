# 朝暮 · DawnDusk — V2 版本设计方案

> 日期: 2026-08-21 | 状态: 待批准 | 基座: couple-tasks-web 单文件 v1 (100/100)

---

## 一、正式名称（用户定稿）

| 项 | 内容 |
|---|---|
| **中文名** | **朝暮** |
| **英文名** | **DawnDusk** |
| **品牌全称** | 朝暮 DawnDusk |
| **主 Slogan** | 两情若是久长时，定不负朝朝暮暮 |
| **英文 Slogan** | Long love keeps every dawn and dusk. |
| **短 Slogan** | 不负朝朝暮暮 |

> 出处: 秦观《鹊桥仙·纤云弄巧》「两情若是久长时，又岂在朝朝暮暮」
> 变义: 原句是"长久相爱岂必每天厮守"；此处反用其意，取**"既然相爱长久，更要珍视每一个朝朝暮暮"**——对应每日约定、正字攒画、天天打卡的产品内核。
> 英文直译: 朝=dawn, 暮=dusk；DawnDusk 一词即"从日出到日落/每一天"，好记且意象完整。

页面标题: `朝暮 DawnDusk` | PWA name: 朝暮 | icon: 💑(或 🌅/🌙 朝暮意象)

---

## 二、信息架构（底部 4 Tab 导航）

```
┌─────────────────────────────┐
│ 今天       日历       回忆       我们 │  ← 底部 Tab
└─────────────────────────────┘
```

### Tab 1 「今天」
- 顶部：**节日横幅**（今天有节日/纪念日 → 横幅 + 动画；无则显示连续打卡天数）
- 每日约定卡（现有，原样保留 + 逻辑打磨）
- **习惯打卡区**（新）：每日可重复项（早睡/喝水/运动…），一天一键，本周 7 格视图
- **正字进度条** + 下方「打一下」悬浮按钮
- 今日提醒聚合：倒计时到期 / 经期预测 / 节日预告

### Tab 2 「日历」（新，去重现有 17 周热力图）
- **月度日历**：当月整月视图，可翻月
  - ✅ = 约定完成 | ⭐ = 节日/纪念日 | 💧 = 经期(可关) | ⏳ = 倒计时到期日 | ❤️ = 打一下统计日
- 点击日期 → 底部弹层详情（当天约定/打卡/节日/经期）
- **「实时」实现**：数据写入后立即重渲染 + 页面 focus/visibilitychange 自动拉取最新 + 手动下拉刷新
  - ⚠️ 诚实说明：纯静态页无推送，GitHub API 配额 60次/小时，不做轮询；"实时"= 每次打开/切回/写入即最新
- 原有 GitHub 风格 17 周热力图保留在「回忆」Tab

### Tab 3 「回忆」
- 回忆墙（现有）+ 17 周热力图（现有）+ 里程碑（正字/打一下/连续天数）

### Tab 4 「我们」
- 名字设置（现有）
- **特殊节日管理**（新）：内置节日库 + 自定义增删
- **长期任务倒计时**（新）
- **经期记录**（新，入口二级、默认关闭）
- **打一下统计**（新）：双方被拍次数、连击、总互动
- 关于 / 隐私说明

---

## 三、新功能设计

### 3.1 重要节日特殊动画
- **内置节日库**（自动触发动画）：
  - 公历：2·14 情人节 / 12·25 圣诞 / 1·1 元旦 / 3·14 白色情人节 / 5·20 网络情人节
  - 农历：七夕(7·7) / 中秋(8·15) / 春节(1·1) / 元宵(1·15) → **内置 2026–2030 农历映射表**（避免引 10KB+ 农历库，含闰月校验）
- 当天打开页面自动触发：彩带 + 烟花 + 节日专属配色 + 祝福文案（可自定义）
- **自定义节日**：名称 + 日期(公历/农历) + emoji + 是否每年重复 + 动画强度(无/轻/盛大) + 留言
- 纪念日（在一起 X 天）自动计算并支持设为节日

### 3.2 长期任务倒计时
- 增加目标：标题 + emoji + 目标日期 + 备注（如「攒钱去上海迪士尼 - 还有 87 天」）
- 展示：今天 Tab 今日提醒 + 日历 Tab 到期日标记
- 只读查询不改变每日约定逻辑；多人共享（双向同步）

### 3.3 每天打卡（习惯打卡）
- 与「每日约定」互补：约定 = 每天共同完成一件事；习惯 = 个人/双方的重复小日常
- 习惯项：名称 + emoji + 归属(共同/仅我/仅 TA) + 每天可打一次
- 展示：本周 7 格横条（✅/❌/未到）
- 数据按天累积，可在日历上回看

### 3.4 经期记录（隐私保护 × 界面位置）
- **入口**：仅「我们」Tab → 二级「经期管理」，主界面零暴露
- **默认关闭**；开启时需一次性确认「数据仅本地/同步给 TA」选择
- **界面标记**：日历上 💧 小图标（可整体关闭显示），不出现任何文字细节
- **存储策略**（默认最隐私）：
  - 默认：仅存 localStorage（完全不离开本机）
  - 可选：加密后同步到私有仓库（AES-GCM，密钥只存本地，服务端不可读）
- **功能**：记录开始日 + 周期天数 + 持续天数 → 自动预测下次（本地计算），提前 1 天日历淡提示
- 双方可见性：默认单方（仅记录者看到），可选共享

### 3.5 「打一下」· 创意互动
- 位置：今天 Tab 右下角悬浮圆钮（💥），不干扰主操作
- 点击：屏幕轻震(vibrate) + 敲击动画（💥 弹跳 + 爱心飞溅/猫爪/拳头，表情随机） + 音效(可选)
- **统计**（同步双端，写入私有仓库）：
  - 「你打了我 X 次 / 我打了你 Y 次」+ 单日最高 + 连击 streak
  - 对方打开页面时若被拍过 → 横幅提示「💥 有人拍了你 3 下！」
- 彩蛋：连续互打 5/25/100 次 → 成就动画（与正字系统呼应）
- 数据：pokes.history 只存日期+计数（不存时间戳细节，减少隐私量）

---

## 四、数据模型（向后兼容，v1 数据零迁移)

```json
{
  "names": {"a":"", "b":""},
  "days": {"YYYY-MM-DD": {...}},          // v1 原样
  "zhengzi": {"count":1, ...},             // v1 原样
  "festivals": [{"id","name","date":"MM-DD","lunar":false,"emoji","repeat":true,"anim":"light|grand","note"}],
  "countdowns": [{"id","title","emoji","target":"YYYY-MM-DD","note"}],
  "habits": [{"id","name","emoji","owner":"both|a|b","marks":["YYYY-MM-DD",...]}],
  "period": {"enabled":false,"owner":"a","visible":"me","history":[],"cycle":28,"duration":5,"encrypted":false},
  "pokes": {"a":{"total":0,"streak":0,"last":"YYYY-MM-DD"},"b":{...},"history":{"YYYY-MM-DD":{"a2b":0,"b2a":0}}}
}
```

## 五、代码结构重构（建议）

```
couple-tasks-web/
├── index.html        # 骨架 (~200 行)
├── css/style.css     # 样式 (~600 行)
├── js/app.js         # 主逻辑 + Tab 路由
├── js/data.js        # 数据层（GitHub API/localStorage/加密）
├── js/calendar.js    # 月历 + 热力图
├── js/anim.js        # 庆祝/节日/打一下动画
└── js/config.js      # 节日库 + 农历表 + 常量
```

> 单文件 48KB → 预计拆分后 6 文件，易维护；Pages 部署无差异。
> v1 单文件保留在 git 历史，可随时回滚。

## 六、整体打磨项

1. 加载态/错误态（GitHub 限流提示、离线降级提示）
2. 状态统一：Tab 切换记忆 + 写入后全量重渲
3. 触控反馈：按钮震动、动画降级（prefers-reduced-motion）
4. 日期逻辑统一（消除"今天/昨天"时区陷阱）
5. 正字格换「爱」字选项 + 礼物名自定义（v1 遗留）
6. 回忆墙性能：虚拟滚动（90+ 天不卡）

## 七、执行流程

1. 批准本方案 → 2. 写实现计划 → 3. 分模块实现（data → calendar → anim → app）→ 4. 本地 headless 截图评测（沿用 V4 评分标准）→ 5. mimo 审批 → 6. 部署 Pages → 7. 端到端验证

## 八、风险与取舍

- 农历映射表只覆盖 2026–2030（够用，后续可扩展）
- 经期加密同步复杂度中等；MVP 先做「仅本地」，加密同步排第二迭代
- 「打一下」音效用 WebAudio 合成（不引音频文件，省体积）
